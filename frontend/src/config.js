export const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:5001/api'; 
// src/config.js
export const USE_MOCK_TASKS = false; // Cambia a false cuando uses el backend real
// src/config.js
const config = {
  USE_MOCK_USER_SERVICE: false,
};

export default config;
